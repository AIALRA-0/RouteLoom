# 1 ADR 0002：PostgreSQL 持久队列

状态：已接受

日期：2026-08-25

## 1.1 背景

首版部署资源有限，需要持久任务、幂等、事件与恢复能力，同时避免增加 Redis 运维面

## 1.2 决策

业务数据使用 PostgreSQL，pg-boss 在同一数据库的独立 Schema 中提供持久队列

Worker 排空队列后升级，数据库迁移使用 expand-contract 顺序

## 1.3 结果

重启不会丢失已接任务，数据库成为需要重点备份与恢复演练的单一状态组件
