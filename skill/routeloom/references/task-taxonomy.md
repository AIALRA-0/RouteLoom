# 1 任务分类

## 1.1 Luna

使用条件同时包括边界清楚、低歧义、结构化输出和自动验证

典型任务包括分类、字段抽取、格式转换、批量摘要和范围固定的小修

## 1.2 Terra

用于日常编码、调试、集成、审查和需要读取项目上下文的实现任务

## 1.3 Sol

用于架构规划、高风险改动、相互冲突的证据和需要裁决的设计选择

## 1.4 验收失败

Schema、`equals` 或 `contains` 检查失败时，调用进入 `failed` 并返回具体检查结果

修改任务合同后可以使用新的幂等键显式重跑，系统不自动升级模型
